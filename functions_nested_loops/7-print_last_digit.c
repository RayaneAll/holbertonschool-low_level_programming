#include "main.h"

/**
 * print_alphabet - imprime alphabet en minucscule, suivi d'un saut de ligne
 */

int print_last_digit(int)
{
	int last_digit;
	int n;

	last_digit = n % 10;
	return (last_digit);
}
